Testing colours

```shell title="Committing to github" linenums="1"
git add .
git commit -m "Initial commit"
git push
```

```bash title="Creating a new branch" linenums="1" hl_lines="1"
git switch -c John

git switch main
git pull origin main
```